# Theme-Switcher-App-
A simple JavaScript project that allows users to switch between **Light**, **Dark**, and **Colorful** themes.   The selected theme is saved using **localStorage**, so it persists even after page refresh.
